#!/usr/bin/env bash
set -euo pipefail

PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"

${PROJECT_DIR}/bin/build-electron-main-prod
${PROJECT_DIR}/bin/build-electron-front-prod
${PROJECT_DIR}/bin/build-stylesheets-prod
